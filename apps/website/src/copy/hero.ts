export const hero = {
	headline: 'See the grid you are lining up against.',
	subline:
		'A column grid over every page while you build it, as a button in Astro’s dev toolbar. Configured in astro.config, and nowhere else.',
	note: 'The grid on this page is the same class the integration uses — not a copy of it.',
} as const;

export const footer = {
	licence: 'MIT licensed. Built by',
} as const;
